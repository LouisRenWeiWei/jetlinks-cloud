spring:
  cloud:
    nacos:
      config:
        server-addr: nacos-server:8848
        file-extension: yml
        shared-dataids: common-config.yml
        refreshable-dataids: device-gateway-service.yml
      discovery:
        endpoint: device-gateway-service
        server-addr: nacos-server:8848