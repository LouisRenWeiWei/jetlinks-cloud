jetlinks:
  redis:
    clients:
      default:  #默认client
        hosts: redis://redis:6379
        database: 0
      device-registry:  #设备注册中心,使用redis数据库1
        hosts: redis://redis:6379
        database: 1
spring:
  rabbitmq:
    addresses: rabbitmq:5672
    password: jetlinks
    username: jetlinks